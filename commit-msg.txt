fix(reservation): handle missing table field gracefully

- Add optional chaining (?.) and nullish coalescing (??) on
  reservation.table in ReservationTable and detail modal
- Update Reservation interface table field to optional
- Fixes "Cannot read properties of undefined (reading 'tableName')"
  crash when reservation has no table assigned
